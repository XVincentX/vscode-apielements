{
  "version": "0.2.0",
  // List of configurations. Add new configurations or edit existing ones.
  "configurations": [
    {
      "name": "Attach",
      "type": "node",
      "request": "attach",
      "sourceMaps": true,
      "port": 6004,
      "outFiles": [
        "${workspaceRoot}/../client/server/*.js"
      ],
      "protocol": "legacy"
    }
  ]
}
